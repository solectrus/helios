<%= helpers.turbo_frame_tag frame_id do %>
  <div class="tooltip tooltip-left before:text-left before:text-xs tooltip-info" data-tip="<%= status_label %>">
    <div class="w-3 h-3 flex items-center justify-center"><span class="<%= status_indicator_class %>"></span></div>
  </div>
<% end %>
