<% content_for :title, t('.title') %>

<%= render "shared/configuration_page", active_tab: :sensors do %>
  <%= render SensorTable::Component.new(
        configuration: @configuration,
        polling_enabled: @polling_enabled,
      ) %>
<% end %>
