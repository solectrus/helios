{
  "locale": "de",
  "title": {
    "de": "MQTT-Collector",
    "default": "MQTT Collector"
  },
  "description": {
    "de": "Liest Messwerte von Topics eines MQTT-Brokers.",
    "default": "Reads measurements from topics on an MQTT broker."
  },
  "pages": [
    {
      "name": "p_mqtt_connection",
      "title": {
        "de": "Verbindung zum MQTT-Broker",
        "default": "Connection to the MQTT broker"
      },
      "elements": [
        {
          "type": "text",
          "name": "mqtt_host",
          "title": {
            "de": "Hostname oder IP-Adresse",
            "default": "Hostname or IP address"
          },
          "isRequired": true,
          "placeholder": {
            "de": "z.B. mqtt.fritz.box",
            "default": "e.g. mqtt.fritz.box"
          }
        },
        {
          "type": "text",
          "name": "mqtt_port",
          "title": {
            "de": "Port (optional)",
            "default": "Port (optional)"
          },
          "description": {
            "de": "Standard: 1883 (oder 8883 bei SSL)",
            "default": "Default: 1883 (or 8883 with SSL)"
          },
          "inputType": "number"
        },
        {
          "type": "boolean",
          "name": "mqtt_ssl",
          "title": {
            "de": "SSL/TLS verwenden",
            "default": "Use SSL/TLS"
          },
          "defaultValue": false
        }
      ]
    },
    {
      "name": "p_mqtt_auth",
      "title": {
        "de": "Authentifizierung beim MQTT-Broker",
        "default": "Authentication with the MQTT broker"
      },
      "elements": [
        {
          "type": "text",
          "name": "mqtt_username",
          "title": {
            "de": "Benutzername (optional)",
            "default": "Username (optional)"
          }
        },
        {
          "type": "text",
          "name": "mqtt_password",
          "title": {
            "de": "Passwort (optional)",
            "default": "Password (optional)"
          },
          "inputType": "password"
        }
      ]
    },
    {
      "name": "p_image",
      "title": {
        "de": "Software-Stand des Collectors",
        "default": "Collector release channel"
      },
      "elements": [
        {
          "type": "radiogroup",
          "name": "image",
          "title": {
            "de": "Welchen Stand möchtest du verwenden?",
            "default": "Which release should be used?"
          },
          "isRequired": true,
          "choices": []
        }
      ]
    }
  ],
  "showQuestionNumbers": "off",
  "completeText": {
    "de": "Speichern",
    "default": "Save"
  },
  "showCompletePage": false
}
